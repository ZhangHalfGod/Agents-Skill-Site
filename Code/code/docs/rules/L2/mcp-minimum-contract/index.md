---
title: "L2 · MCP 最低契约"
description: 本仓 docs 真源；直接编辑本页后 npm run generate
---

# L2 · MCP 最低契约

1. `/capabilities` 中工具定义必须与 `/tools/call` 实际行为一致。
2. 每个工具必须具备完整 description 与 parameters。
3. 错误语义稳定，避免同一错误场景返回不一致结构。

## 元数据

- 层级：`L2`
- 文件：`mcp-minimum-contract.mdc`
- alwaysApply：`false`
- 真源：本页（`docs/…`）

## 在 Cursor 中使用

`@docs/rules/L2/mcp-minimum-contract/index.md`
